const STATUS_CODES = {
  OK: 200,
};

const HREF_ELEMENTS = {
  DELEGATE: "delegate",
  CLAIM: "claim",
  BRIDGE: "bridge",
  EARN: "lp.kima",
  ECOSYSTEM: "ecosystem",
  TUTORIALS: "tutorials",
};

const URL_PATHS = {
  BLOCKS: /blocks/,
  SARDIS: /graphql.sardis.kima.network/,
};

const DATA_TYPES = {
  STRING: "string",
  NUMBER: "number",
  OBJECT: "object",
};

export { STATUS_CODES, HREF_ELEMENTS, URL_PATHS, DATA_TYPES };
